import 'package:flutter/material.dart';

class CO2 extends StatelessWidget {
  static String routeName = "/co2";
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: const Text("CO2"),
      ),
    );
  }
}
